# Notion templates

These markdown files are meant to be copied into Notion pages or imported as starter content.

## Suggested usage

1. Create a top-level Notion page named `avatar_ai`
2. Create sub-pages that match the files in `templates/`
3. Copy the content from each template into the matching page
4. Turn the listed schema sections into Notion databases
